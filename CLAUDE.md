# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

PyQt6 desktop app ("GoonerApp", GitHub repo name `AutoShowStroker`) that turns a local folder of images/GIFs/videos into a randomized slideshow synced to a rhythm generator ("Strokemeter"), with multilingual text callouts. Entry point: `python main.py`.

## Architecture

- `src/GoonerApp.py` — main window/GUI. Layout, keyboard shortcuts, media playback (`QLabel` for images, `QMediaPlayer`/`QVideoWidget` for video), folder loading, playlist shuffling, autoplay timers.
- `src/BeatHandler.py` — rhythm engine: beat patterns, audio feedback, visual blink, random pause phases.
- `src/CalloutHandler.py` — loads localized text lines from `res/callouts/<lang>.json`, keyed by trigger event (`beat_change_general`, `beat_change_faster/slower`, `pause_start`, `pause_end`, `media_skipped`, `media_repeated`, `session_started`), fires random matching phrases.
- `src/ScoreTracker.py` — session stats (beat count, pause count/duration, favorite pattern, skips/repeats, runtime).
- `src/SettingsDialog.py` / `src/StatisticsDialog.py` — settings UI and end-of-session stats UI.
- Components communicate via PyQt signals/slots.
- `src/utils.py` — `get_project_root()` resolves the project root correctly both when run as a script and when frozen by PyInstaller (via `sys._MEIPASS`). Any code that reads files under `res/` must go through this, not a hardcoded/relative path — hardcoding breaks the packaged `.exe`.
- No `__init__.py` in `src/` — it's imported as `src.X` and relies on being run from the repo root (implicit namespace package), not installed as a package.

## Build

Packaged with PyInstaller into a single windowed executable:
```
pyinstaller --noconfirm --onefile --windowed --add-data "res;res" --name "GoonerApp" main.py
```
Output: `dist/GoonerApp.exe`. `GoonerApp.spec` is gitignored (regenerated by the command above, not checked in).

## Linting

`ruff check .` (config in `ruff.toml`). Install dev deps with `pip install -r requirements-dev.txt`. As of this writing there are ~18 pre-existing lint findings in `src/` — not yet cleaned up, don't treat them as something every PR must fix unless the PR is already touching that file.

## Testing

There is no automated test suite yet (planned, not built). Do not claim a change is verified based on a test run — ask the user to manually test in the running app instead. A `python -m py_compile` hook catches basic syntax errors on edited files, but that is not a substitute for functional verification.

## Git workflow

Use feature branches + PRs for changes, not direct commits to `main` — this is a deliberate move away from older direct-to-main history in this repo, so don't treat past commits as precedent. Never push to the remote (`origin`) without explicit user approval for that specific push. Do not add Claude/AI attribution (e.g. "Co-Authored-By: Claude") to commit messages in this repo.

## Content note

`res/callouts/*.json` contain sexually explicit text (this is an adult-themed app by design) — expected content when editing those files.

## Naming

The local folder is `Gooner`, the GitHub repo is `AutoShowStroker`, and the app itself is `GoonerApp`. All three refer to the same project.
